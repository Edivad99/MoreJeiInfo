name: Release

on:
  push:
    tags:
      - '*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CURSEFORGE_TOKEN: ${{ secrets.CURSEFORGE_TOKEN }}
      MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'adopt'
          java-version: 17
          cache: 'gradle'

      - name: Build with Gradle
        run: ./gradlew build --no-daemon --max-workers 1

      - name: Make Changelog
        run: ./gradlew common:makeChangelog --no-daemon --max-workers 1

      - name: Publish
        run: |
          - rm -f forge/build/libs/*
          - ./gradlew forge:publishMods --no-daemon --max-workers 1
          - rm -f fabric/build/libs/*
          - ./gradlew fabric:publishMods --no-daemon --max-workers 1
